<h1>Offers <a id="addOffer" class="text-success" >⊞</a></h1>
{% if offers is defined and offers is not null %}
    <table class="table table-hover table-striped bg-light">
        <thead>
        <th>Title</th>
        <th>By</th>
        <th>Time</th>
        <th>Amount</th>
        <th>Insurance</th>
        </thead>
        {% for offer in offers %}
            <tr><td>{{ offer.ticket.title }}</td>
                <td>{{ offer.user.username }}</td>
                <td>{{ offer.time.date|date('H:i')}}</td>
                <td>{{ offer.amount }}</td>
                <td>{% if offer.insurance == 1 %}
                        <p class="text-success">YES</p>
                        {% else %}
                        <p class="text-danger">NO</p>
                {% endif %}
            </tr>
        {% endfor %}
    </table>
{% endif %}
